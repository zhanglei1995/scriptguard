/**
 * @scriptguard/shared
 * 扩展与服务端共享的类型/Schema/常量
 *
 * 当前状态: 占位包,待从 apps/server/src/lib/schemas.ts 抽离 zod schema
 * 关联: TDD §3.1.3 (Schema Sharing)
 */

// 占位导出,确保路径可解析;后续在此追加共享类型。
export const SHARED_PACKAGE_PLACEHOLDER = true